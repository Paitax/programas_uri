#include <stdio.h>    

int main()
{     
	int codigo, quant;    
	
	scanf("%d", &codigo);
	scanf("%d", &quant);       
	
	if (codigo == 1)
		printf("Total: R$ %.2f\n", 4.00*quant);
		
	else  if (codigo == 2)
		printf("Total: R$ %.2f\n", 4.50*quant);
		
	else if (codigo == 3)
		printf("Total: R$ %.2f\n", 5.00*quant);
	
	else  if (codigo == 4)
		printf("Total: R$ %.2f\n", 2.00*quant);
		
	else  if (codigo == 5)

		printf("Total: R$ %.2f\n", 1.50*quant);
		
	return 0;
} 
