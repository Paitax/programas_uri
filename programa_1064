#include <stdio.h>

int main() 
{
  int i=0, pos=0;
  double x,soma=0;

  for (i = 0; i < 6; ++i) {

  scanf("%lf", &x);

    if (x > 0){
    pos++;
    soma += x;
    }
    }
    printf("%d valores positivos\n", pos);
    printf("%.1lf\n",soma/pos);

    return 0;
}
