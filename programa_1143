#include <stdio.h>

int main()
{
    
    int x, i , a, b, c;

    scanf("%d", &x);

    for (i = 1; i <= x; i++)
    {
      a = i;
      b = i *i;
      c = i * i *i;
      printf("%d %d %d\n", a, b, c);
    }
    
    return 0;
}
