#include <stdio.h>  
  
int main()  
{
	float velocidade, tempo, consumo;

    scanf("%f", &velocidade);
    scanf("%f", &tempo);

    consumo=(velocidade * tempo)/12;
    printf("%.3f\n", consumo);
    
    return 0;
}
