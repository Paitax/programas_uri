#include <stdio.h>
 
int main() {
	double A, B, C, MEDIA;
	printf("");
	scanf("%lf",&A);	
	printf("");
	scanf("%lf",&B);
	printf("");
	scanf("%lf",&C);
	MEDIA=(A*2+B*3+C*5)/10;
    printf("MEDIA = %1.1lf\n", MEDIA);
	
    return 0;
}
