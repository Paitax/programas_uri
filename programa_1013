#include <stdio.h>  
  
int main()
{     
	int A, B, C, X;    

	scanf("%d", &A);
	scanf("%d", &B);
	scanf("%d", &C);
	
	X = (A+B+abs(A-B)) / 2;      
	X = (X+C+abs(X-C)) / 2;    
	  
	printf("%d eh o maior\n", X);
	
	 return 0;
} 
