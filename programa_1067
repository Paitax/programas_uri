#include <stdio.h>

int main()
{
    int x, i, a, b, c;

    scanf("%d", &x);

    for (i = 1; i <= x; i++)
    {
      if (i%2 == 1)
      {
        printf("%d\n", i);
      }
    }

    return 0;
}
