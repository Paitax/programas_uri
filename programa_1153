#include <stdio.h>

int main(){
    
    int x, i, fatorial;
    
    scanf("%d",&x);
    fatorial = 1;

    for (i = 2; i <= x; i++)
    {
    fatorial *= i;    
    }

    printf("%d\n",fatorial);
    
  return 0;   
}
